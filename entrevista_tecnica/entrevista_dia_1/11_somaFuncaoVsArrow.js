/**
 * Exercício 11 — function declaration vs arrow function
 *
 * Implemente a função `soma(a, b)` de duas formas:
 * 1) Como function declaration: function soma(a, b) {}
 * 2) Como arrow function: const soma = (a, b) => {}
 *
 * Depois, explique em voz alta (ou em comentário) a diferença entre
 * as duas formas: hoisting, `this`, uso como método de objeto, etc.
 */

// 1) function declaration
function somaDeclaracao(a, b) {
  // implemente aqui
}

// 2) arrow function
const somaArrow = (a, b) => {
  // implemente aqui
};

console.log(somaDeclaracao(2, 3));
console.log(somaArrow(2, 3));

/**
 * Explicação (escreva aqui com suas palavras):
 *
 */
