/**
 * Exercício 20 — Recursão: fatorial e Fibonacci
 *
 * 1) Escreva `fatorial(n)` recursiva.
 *    fatorial(5) = 5 * 4 * 3 * 2 * 1 = 120
 *
 * 2) Escreva `fibonacci(n)` recursiva.
 *    fibonacci(0) = 0, fibonacci(1) = 1, fibonacci(n) = fibonacci(n-1) + fibonacci(n-2)
 *
 * Para cada uma, identifique e explique o CASO BASE (a condição que
 * para a recursão).
 *
 * Exemplo:
 * fatorial(5) // 120
 * fibonacci(6) // 8
 */

function fatorial(n) {
  // implemente aqui
}

function fibonacci(n) {
  // implemente aqui
}

console.log(fatorial(5));
console.log(fibonacci(6));

/**
 * Caso base de cada função (escreva aqui com suas palavras):
 *
 */
